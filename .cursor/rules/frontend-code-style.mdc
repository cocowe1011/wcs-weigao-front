---
description: 前端工程规范与代码风格（基于项目扫描归纳）
globs: **/*.{vue,js,less}
alwaysApply: true
---

# WCS 前端工程规范与代码风格

> 本文档基于项目代码扫描归纳，仅包含纯技术向的前端规范，不涉及业务逻辑。

---

## 一、组件写法

### 1.1 API 使用偏好

- **统一使用 Vue 2 选项式 API（Options API）**，不使用 Composition API
- 组件导出格式：`export default { name, components, props, data, watch, computed, methods, created, mounted, beforeDestroy }`
- 生命周期钩子按固定顺序：`created` → `mounted` → `beforeDestroy`（如有）

### 1.2 组件结构顺序

```javascript
export default {
  name: 'ComponentName',
  components: {},
  props: {},
  data() {
    return {};
  },
  watch: {},
  computed: {},
  methods: {},
  created() {},
  mounted() {},
  beforeDestroy() {}  // 用于清理监听器、定时器等
};
```

### 1.3 组件拆分原则

- **页面级组件**：放在 `src/views/` 下，按功能模块分目录（如 `login/`、`home/`）
- **可复用组件**：放在 `src/components/`，如 `StatusMonitoring.vue`、`OrderQueryDialog.vue`
- **弹窗/对话框**：独立为子组件，通过 `props.visible` + `$emit('update:visible')` 控制显隐
- **大型页面**：可将样式抽离到同目录 `css/*.less`，通过 `@import` 引入

### 1.4 命名约定

- 组件名：PascalCase（如 `OrderQueryDialog`、`StatusMonitor`）
- 路由组件名与 `name` 保持一致（如 `MainPage`、`WelcomPage`）

---

## 二、样式规范

### 2.1 预处理器与作用域

- **统一使用 Less**，`<style lang="less" scoped>`
- 根级样式使用 `#app` 或页面根 class，避免污染全局

### 2.2 命名风格

- **BEM 风格**：块-元素-修饰符，如 `plc-panel__content`、`plc-panel__actions`、`plc-panel__row`
- **连字符命名**：`section-header`、`scrollable-content`、`data-card-border`
- **嵌套结构**：使用 `&-top`、`&-down` 等 Less 父选择器引用

```less
.login {
  &-left { }
  &-right {
    &-top { }
    &-down { }
  }
}
```

### 2.3 样式组织

- 大块样式可抽离到 `views/xxx/css/xxx.less`，主组件通过 `@import` 引入
- 深度选择器：使用 `::v-deep` 覆盖 Element UI 组件样式

```less
::v-deep .el-input__inner {
  height: 45px;
}
```

### 2.4 UI 组件库

- **Element UI 2.x**：全局引入 `Vue.use(ElementUI)`，样式全局引入
- 常用组件：`el-input`、`el-button`、`el-dialog`、`el-table`、`el-select`、`el-menu`、`el-dropdown`、`el-form`、`el-tag`、`el-pagination`、`el-avatar`
- 图标：`el-icon-xxx`（如 `el-icon-search`、`el-icon-close`）

---

## 三、网络请求

### 3.1 封装格式

- **双实例**：按后端服务拆分
  - `HttpUtil` → `VUE_APP_BASE_URL`（主业务 API）
  - `HttpUtilwms` → `VUE_APP_BASE_URL_WMS`（WMS 专用）
- **创建方式**：`axios.create({ baseURL, timeout: 10000 })`

### 3.2 响应拦截

- 成功：`return response.data`，业务层直接拿到 `data` 层数据
- 失败：`return Promise.reject(error)`，不在此处统一弹窗，由调用方处理

### 3.3 调用习惯

- 直接 `import HttpUtil from '@/utils/HttpUtil'`
- 使用 `HttpUtil.post(path, params)` 或 `HttpUtil.get(path)`
- 接口路径：REST 风格，如 `/userInfo/save`、`/order/selectListByPage`、`/queue_info/update`

### 3.4 异常与提示

- **成功**：`this.$message.success('xxx')` 或 `this.$notify({ title, message, type: 'success', duration: 2000 })`
- **失败**：`this.$message.error('xxx')` 或 `this.$message.warning('xxx')`
- **校验**：表单校验失败时 `this.$message.error('xxx')` 并 `return false`
- **异步**：`.then().catch()` 或 `try/catch` + `this.$message.error()`，在 `catch` 中统一提示

```javascript
HttpUtil.post('/xxx', param)
  .then((res) => {
    if (res.data) {
      this.$message.success('操作成功');
    } else {
      this.$message.error(res.message || '操作失败');
    }
  })
  .catch((err) => {
    this.$message.error('操作失败，请重试');
  });
```

---

## 四、路由与状态

### 4.1 路由配置

- **模式**：`mode: 'hash'`，`base: process.env.BASE_URL`
- **结构**：顶层路由 + 嵌套 `children`，主布局 `HomePage` 作为父级
- **懒加载**：子路由使用 `() => import('../views/home/xxx.vue')`
- **命名**：`path` 与 `name` 对应，如 `MainPage`、`WelcomPage`、`SterilizationMonitor`、`AboutPage`

### 4.2 导航方式

- 使用 `this.$router.replace({ path: '/homePage/xxx' })` 进行页面切换
- 跳转前可用 `this.$nextTick()` 确保 DOM 更新后再路由

### 4.3 状态管理

- **Vuex**：项目已配置但 Store 为空（`state/getters/mutations/actions/modules` 均为空）
- **实际状态**：以组件 `data` 为主，跨组件共享通过：
  - Electron 全局：`remote.getGlobal('sharedObject').userInfo`
  - 父子组件：`props` + `$emit`
  - 事件总线：`EventBus`（如有）

---

## 五、其他约定

### 5.1 代码风格（ESLint）

- 分号：`semi: 'off'`（项目未强制分号）
- 单引号：Prettier 默认
- `camelcase`、`no-unused-vars`：关闭
- 生产环境：`no-console`、`no-debugger` 为 `warn`

### 5.2 资源引用

- 图片：`require('@/assets/xxx.png')` 或相对路径 `./img/xxx.png`
- 别名：`@` 指向 `src/`

### 5.3 Electron 相关

- IPC：`ipcRenderer.send()`、`ipcRenderer.on()`、`ipcRenderer.invoke()`
- 组件销毁时需 `ipcRenderer.removeListener()` 和 `clearTimeout`/`clearInterval` 防止内存泄漏
